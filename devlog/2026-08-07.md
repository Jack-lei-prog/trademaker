# 开发日志 - 2026-08-07

## 完成事项

### 安全加固 (P0)
- [x] 新建 `config.py` 集中配置模块，启动时校验 `SECRET_KEY`
- [x] 新建 `auth_middleware.py` JWT 认证中间件
  - `create_token()` / `decode_token()` — JWT HS256 签发与验证
  - `@login_required` — 装饰器，30 天过渡期兼容 body `user_email` 回退
  - `@optional_login` — 可选认证装饰器
- [x] 修复 `app.py` CORS — 删除 `_is_dev` 通配逻辑，所有环境使用显式白名单 (`CORS_ORIGINS` 环境变量)
- [x] `app.py` 设置 `app.secret_key` 并添加安全响应头 (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy)
- [x] `app.py` 新增 OPTIONS 预检处理器
- [x] 新建 `smtp_crypto.py` Fernet SMTP 密码加密模块
- [x] `smtp_config.py` 加密存储密码，自动迁移旧明文
- [x] `auth_bp.py` 注册/登录返回 JWT token
- [x] `auth_bp.py` 移除 login() 中 hardcoded demo 后门
- [x] `.env` 新增 `SECRET_KEY`
- [x] `.env.example` 更新 (SECRET_KEY, CORS_ORIGINS, SMTP_ENCRYPTION_KEY, TRUSTED_PROXIES)

### 邮件安全 + 限流 (P1)
- [x] `email_bp.py` 新增两阶段邮件确认发送:
  - `POST /api/email/draft` — 生成草稿（不发送）
  - `POST /api/email/draft/<id>/send` — 确认发送（需 `idempotency_key` 防重复）
- [x] 全端点限流覆盖:
  - `email_bp`: smtp_send, smtp_settings, smtp_test, sent, pending, status, stats, sync, classify
  - `inquiry_bp`: process, pending, alerts
  - `evaluate_bp`: evaluate, improve, kimi, dual
  - `dashboard_bp`: customer-acquisition
  - `chat_bp`: upload_manual, upload_excel
- [x] 敏感端点加 `@login_required`:
  - email_bp (全部), contact_bp (全部), dashboard_bp (全部)
  - inquiry_bp (全部), evaluate_bp (全部)
- [x] `chat_bp.py` 文件上传 10MB 大小限制
- [x] `security.py` RateLimitStore 抽象层（为 Redis 迁移做准备）

### 文档 + 数据可信度 (P2)
- [x] `README.md` 完全重写 — 12 工具准确列表，API 端点表，架构图，数据源说明，安全特性
- [x] `data_sources.py` 新增 `enrich_result()` 辅助函数 — 所有结果统一携带 `source`, `source_url`, `fetched_at`, `confidence`
- [x] `data_sources.py` 新增 `DATA_SOURCE_STATUS` 降级标记
- [x] `tools.py` `search_buyers()` 返回添加 `fetched_at` 和降级警告
- [x] `chat_bp.py` `/api/health` 返回数据源状态
- [x] `chat_bp.py` `/api/demo/help` 密码改为 `demo_account_hint`（不返回裸露密码）

### 测试 + 审计 (P3)
- [x] `tests/test_security.py` 16 个安全测试:
  - TestAuthRequired (7 个端点)
  - TestCrossUserAccess (2 个场景)
  - TestJWTToken (3 个场景)
  - TestSMTPEncryption (2 个场景)
  - TestSecurityHeaders (2 个场景)
- [x] `logger.py` SensitiveDataFilter 自动遮盖 Bearer token, password, api_key
- [x] 新建 `demo_walkthrough.py` 端到端演示脚本

### 测试结果
- 74/74 测试通过 (58 原有 + 16 新增安全测试)

## 待办事项

### 后续迭代
- [ ] 前端适配 JWT — 登录后存储 token，所有请求发送 `Authorization: Bearer <token>` 头
- [ ] 前端邮件两阶段 UI — 草稿预览 → 确认发送按钮 + idempotency_key 生成
- [ ] Redis RateLimitStore 实现（多 worker 部署时替换内存版）
- [ ] 30 天过渡期后移除 `@login_required` 的 body `user_email` 回退

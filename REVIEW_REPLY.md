# TradeMaster 评审意见回复

尊敬的评审老师：

感谢您非常细致和专业的评审意见，让学生受益匪浅。以下针对您指出的各项问题逐一汇报修改情况。

---

## 一、关于登录接口缺乏服务端身份授权（3.1）

**学生的理解：** 您指出之前的登录接口只返回用户信息，没有签发任何身份凭证，后续接口全靠客户端自行提交 `user_email` 来表明身份，这等于任何人可以冒充任何用户。这是一个非常严重的安全问题。

**修改情况：** 已完全修复。

- 新增 `auth_middleware.py` 模块，实现了 JWT 令牌的签发和验证功能
- 注册和登录接口成功后会返回 `token` 字段
- 编写了 `@login_required` 装饰器，从请求头的 `Authorization: Bearer <token>` 中提取当前用户身份，不再信任客户端提交的 `user_email`
- 将 `@login_required` 加到了所有涉及用户数据的接口上（SMTP配置、邮件发送、联系人、仪表盘、询盘处理、评价等），仅 `/api/chat`、`/api/health` 等不需要用户身份的接口保持公开
- 新增了跨用户隔离的安全测试，验证用户 A 无法读取用户 B 的数据

## 二、关于邮箱和商业事实的可信度问题（3.2）

**学生的理解：** 您发现了三个严重问题：一是 LLM 降级搜索时要求模型"必须给每家公司返回邮箱"，这会诱导模型编造地址；二是开发信的降级模板里默认写了"十年出口经验"等未经用户确认的声明；三是推测邮箱没有被阻断发送。这些都会导致虚假信息进入真实业务流程。

**修改情况：** 三个问题均已修复。

- **LLM 邮箱编造问题：** 修改了 `search_companies_llm()` 中发给模型的要求，从原来的"每家公司必须返回邮箱，可以用 purchasing@域名 等常见格式推测"，改为"如果不知道公司真实邮箱，请留空。空邮箱比错误邮箱更好。绝不编造"
- **降级模板虚假声明问题：** 删除了开发信降级模板中"We have over 10 years of export experience"的虚假资历描述，替换为通用的"We specialize in manufacturing and exporting high-quality products"
- **推测邮箱阻断问题：** 在 `send_email()` 中强化了邮箱验证标记，当检测到 `purchasing@`、`info@` 等推测邮箱时，设置 `email_verified: false`，明确提示用户需要验证后再发送

另外，之前第一轮安全加固中已经为所有数据源结果添加了 `source`（来源）、`source_url`（原始链接）、`fetched_at`（获取时间）、`confidence`（置信度）四个溯源字段，LLM 生成的结果标注 `confidence: 0.50`。

## 三、关于多 Agent 协作实际是单次关键词路由（3.3）

**学生的理解：** 您一针见血地指出了之前架构的核心缺陷。之前的 `detect_intent()` 只是用第一个匹配的关键词来选一个 Agent，之后整个对话都固定使用这个 Agent 的工具集。所以"搜索德国LED进口商并给他们写开发信"这样的请求，会因为只匹配到"搜索"关键词而使用 buyer_agent，但这个 agent 根本没有 `draft_email` 工具，无法完成写开发信的任务。

**修改情况：** 已重构。

- 新增了 `detect_intents()` 函数，返回所有匹配到的意图及其顺序，例如"搜索德国LED进口商并给他们写开发信"会检测到两个意图：`buyer_agent`（搜索）和 `email_agent`（开发信）
- 新增了 `get_task_agents()` 函数，将多意图拆解为结构化的子任务计划
- 当检测到多个意图时，Agent 循环自动切换为 coordinator 角色，coordinator 拥有所有工具，通过升级后的 `MULTI_AGENT_PROMPT` 引导 LLM 按步骤切换角色
- 多步骤任务的迭代上限自动按意图数量缩放，避免因步骤增多而超时
- 响应中增加了 `task_plan` 和 `agent_meta` 字段，方便前端展示任务进度和 Agent 状态

## 四、关于展会情报缺乏来源和有效期（3.4）

**学生的理解：** 您指出之前展会数据库里的日期、规模、认证费用等都是硬编码的，没有来源链接，也没有核验时间。更严重的是，过期的展会没有被过滤，可能会推荐已经结束的活动给用户。

**修改情况：** 已修复。

- 新增了 `_parse_tradeshow_date_range()` 函数，解析展会日期字符串，提取起始和结束日期
- 新增了 `_enrich_tradeshow()` 函数，为每条展会自动注入元数据：数据来源、原始链接、核验时间、展会起止日期、当前状态（即将举行/正在进行/已结束）
- `find_tradeshows()` 函数现在默认过滤掉 `status: ended` 的过期展会
- 如需查看历史展会，可通过 `include_expired=True` 参数获取

## 五、关于项目文档不一致（3.5）

**学生的理解：** 您指出 README、DEMO 文档等与实际代码不一致，外部人员无法从文档准确判断项目的真实能力。

**修改情况：** 已修正。

- README.md 完全重写，现在的版本准确列出了 12 个工具、完整架构图、30+ 端点 API 表、数据源置信度表、环境变量表
- 新增 `demo_walkthrough.py` 演示脚本，覆盖登录、搜索买家（查看来源）、分析公司、生成开发信草稿（两阶段流程）、仪表盘、跨用户隔离验证、未认证访问拦截等 8 个步骤
- 当前全部 74 个测试通过（58 个原有测试 + 16 个安全测试）

---

## 总结

再次感谢评审老师的悉心指导。学生深刻认识到，一个面向真实业务的外贸 Agent，最重要的不是功能的堆砌，而是用户身份的可靠保护、数据来源的可信保障，以及系统不会将推测内容包装成确定事实。

经过两轮修改，以上问题均已得到解决。学生会在今后的开发中谨记老师提出的原则：身份认证是第一道防线，数据可信度是业务底线，任务编排决定了系统的实际能力上限。
